import { NodeWidget, QWidget } from './QWidget';
export declare abstract class QAbstractScrollArea extends NodeWidget {
    viewportWidget?: NodeWidget;
    setViewport(widget: NodeWidget): void;
    viewport(): QWidget;
}
