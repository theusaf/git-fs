import { Component } from '../core/Component';
import { NodeWidget } from './QWidget';
export declare abstract class NodeLayout extends Component {
    type: string;
    abstract addWidget(childWidget: NodeWidget, ...args: any[]): void;
    activate: () => boolean;
    invalidate(): void;
    update(): void;
}
