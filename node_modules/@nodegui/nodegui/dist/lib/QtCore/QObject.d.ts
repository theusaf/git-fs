import { EventWidget } from '../core/EventWidget';
import { NativeElement } from '../core/Component';
export declare abstract class NodeObject extends EventWidget {
    inherits(className: string): boolean;
    setProperty(name: string, value: any): boolean;
    setObjectName(objectName: string): void;
    objectName(): string;
}
export declare class QObject extends NodeObject {
    native: NativeElement;
    constructor(arg?: NodeObject | NativeElement);
}
